apiVersion: v1
kind: Namespace
metadata:
  name: juice-shop
  labels:
    app.kubernetes.io/part-of: secure-k8s-lab
    # Enforce a restricted Pod Security Standard — defense in depth for a
    # deliberately vulnerable workload.
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/warn: restricted
